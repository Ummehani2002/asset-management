services:
  - type: web
    name: final-asset
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false

databases:
  - name: finalassetdb
    plan: free
    databaseName: final_asset
    user: finalassetuser