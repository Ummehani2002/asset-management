; PHP settings for file uploads (employee/entity import)
; Works on hosts that support .user.ini (e.g. shared hosting, some cloud)
; Laravel Cloud/Nixpacks may use different config - see DEPLOY_TO_NEW_LARAVEL_CLOUD.md
upload_max_filesize = 20M
post_max_size = 25M
max_execution_time = 120
